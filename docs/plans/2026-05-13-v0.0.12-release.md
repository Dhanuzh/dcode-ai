# v0.0.12 Release Plan (2026-05-13)

## Goal

Publish `v0.0.12` with the latest startup-approval and question-deadlock fixes.

## Plan

1. Bump workspace package version:
   - `Cargo.toml` `workspace.package.version` -> `0.0.12`.
2. Keep installer fallback aligned to latest prior release:
   - `install.sh` `FALLBACK_VERSION` -> `0.0.11`.
3. Regenerate lock metadata:
   - Run cargo checks/tests so `Cargo.lock` internal package versions move to `0.0.12`.
4. Validate:
   - `cargo fmt --all -- --check`
   - `cargo test -p dcode-ai-cli`
5. Publish:
   - Commit changes.
   - Create annotated tag `v0.0.12`.
   - Push `main` and tag to `origin`.
