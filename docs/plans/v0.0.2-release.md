# dcode-ai v0.0.2 Release Plan

## Goal

Ship `v0.0.2` so installer latest flow resolves a newer release than `v0.0.1`.

## Steps

- [x] Bump workspace version from `0.0.1` to `0.0.2`.
- [x] Ensure installer fallback version is `0.0.2`.
- [x] Fix release workflow install-script update step to patch `FALLBACK_VERSION` (not removed `VERSION` key).
- [x] Run validation (`cargo fmt --check`, `cargo clippy --workspace -- -D warnings`, `cargo test --workspace`).
- [ ] Commit and push to `main`.
- [ ] Create and push tag `v0.0.2` to trigger GitHub Release workflow.

## Verification

- `curl -sSL https://raw.githubusercontent.com/Dhanuzh/dcode-ai/main/install.sh | bash` should report `Installing dcode-ai v0.0.2` when API is unavailable (fallback path).
- After GitHub release for `v0.0.2` is published, installer latest path should resolve `v0.0.2`.
