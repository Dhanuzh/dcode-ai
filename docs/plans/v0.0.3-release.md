# dcode-ai v0.0.3 Release Plan

## Goal

Publish the latest UI response rendering improvements via GitHub Actions release pipeline.

## Steps

- [x] Bump workspace version from `0.0.2` to `0.0.3`.
- [x] Bump installer fallback version to `0.0.3`.
- [x] Run validation gates (`cargo fmt --check --all`, `cargo clippy --workspace -- -D warnings`, `cargo test --workspace`).
- [ ] Commit and push release-prep changes to `main`.
- [ ] Create and push tag `v0.0.3` to trigger Release workflow.
- [ ] Verify release workflow success and `releases/latest = v0.0.3`.
